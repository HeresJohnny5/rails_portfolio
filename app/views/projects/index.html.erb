<h1>Testing</h1>
<br />
<% @projects_items.each do |project_item| %>
  <p><%= project_item.title %></p>
  <p><%= project_item.subtitle %></p>  
  <p><%= project_item.body %></p>
  <%= image_tag project_item.thumb_image unless project_item.thumb_image.nil? %>
<% end %>