<%= image_tag @project_item.main_image unless @project_item.main_image.nil? %>

<h1><%= @project_item.title %></h1>

<p>
  <em><%= @project_item.body %></em>
</p>

<%= link_to "Back", projects_path %>
